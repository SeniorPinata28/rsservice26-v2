import Image from 'next/image';import Header from '../../../components/Header';import Footer from '../../../components/Footer';import {parts} from '../../../data';
export function generateStaticParams(){return parts.map(p=>({sku:p.sku}))}
export default async function Page({params}){const {sku}=await params;const p=parts.find(x=>x.sku===sku);if(!p)return <><Header/><main className="main"><h1>Запчасть не найдена</h1></main><Footer/></>;return <><Header/><main className="main"><section className="hero"><span className="badge">{p.category}</span><h1>{p.name}</h1><p>{p.compatibility}</p></section><section className="section split"><article className="card partDetailCard"><div className="partDetailVisual"><Image src={p.image} alt={p.name} width={700} height={520} priority/></div><h2>Характеристики</h2><p>Артикул: <b>{p.sku}</b></p><p>Совместимость: <b>{p.compatibility}</b></p><p>Наличие: <b>{p.stock} шт.</b></p><p className="price">{p.price}</p></article><article className="card"><h2>Как заказать</h2><p className="muted">Добавьте товар в подбор или отправьте заявку менеджеру. Финальную цену и наличие подтверждает менеджер.</p><a className="btn primary" href="/parts">Вернуться в каталог</a> <a className="btn" href="/contact">Связаться</a></article></section></main><Footer/></>}
