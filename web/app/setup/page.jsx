import Header from '../../components/Header';import Footer from '../../components/Footer';
export default function Page(){const hasToken=Boolean(process.env.TELEGRAM_BOT_TOKEN);const hasChat=Boolean(process.env.TELEGRAM_CHAT_ID);return <><Header/><main className="main"><section className="hero"><span className="badge">Настройка</span><h1>Статус подключения сайта</h1><p>Эта страница показывает, какие серверные переменные уже подключены на Vercel.</p></section><section className="section grid"><article className="card"><h2>Telegram-уведомления</h2><p>BOT TOKEN: <b>{hasToken?'подключён':'не подключён'}</b></p><p>CHAT ID: <b>{hasChat?'подключён':'не подключён'}</b></p><p className="muted">Для включения уведомлений добавьте в Vercel переменные TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID, затем сделайте Redeploy.</p></article><article className="card"><h2>Заявки</h2><p>API заявок: <b>подготовлен</b></p><p className="muted">Форма отправляет данные на /api/leads. Если Telegram-переменные подключены, заявка уйдёт менеджеру.</p></article><article className="card"><h2>Оплата</h2><p><b>не подключена</b></p><p className="muted">Финансовые настройки не включались.</p></article></section></main><Footer/></>}
