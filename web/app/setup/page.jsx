import Header from '../../components/Header';import Footer from '../../components/Footer';
export default function Page(){const hasToken=Boolean(process.env.TELEGRAM_BOT_TOKEN||process.env.BOT_TOKEN);const hasChat=Boolean(process.env.TELEGRAM_CHAT_ID||process.env.MANAGER_CHAT_ID);return <><Header/><main className="main"><section className="hero"><span className="badge">Настройка</span><h1>Статус подключения сайта</h1><p>Проверка основных рабочих контуров: заявки, Telegram, оплата и следующий этап.</p></section><section className="section grid"><article className="card"><h2>Telegram-уведомления</h2><p>BOT TOKEN: <b>{hasToken?'подключён':'не подключён'}</b></p><p>CHAT ID: <b>{hasChat?'подключён':'не подключён'}</b></p><p className="muted">Если оба пункта подключены, заявки с сайта уходят менеджеру в Telegram.</p></article><article className="card"><h2>Заявки</h2><p>API заявок: <b>работает</b></p><p>Проверка API: <a className="btn" href="/api/leads">Открыть /api/leads</a></p><p className="muted">Сейчас заявки уходят в Telegram и дополнительно сохраняются в браузерной демо-админке.</p></article><article className="card"><h2>Следующий безопасный этап</h2><p><b>Настоящая база заявок</b></p><p className="muted">Для этого нужно подключить Vercel Postgres / Neon / Supabase. Оплата и домен пока не трогаются.</p></article><article className="card"><h2>Оплата</h2><p><b>не подключена</b></p><p className="muted">Финансовые настройки не включались.</p></article></section></main><Footer/></>}
