import Header from '../../components/Header';import Footer from '../../components/Footer';import {services} from '../../data';
export default function Page(){const groups=[...new Set(services.map(s=>s.group))];return <><Header/><main className="main"><section className="hero"><span className="badge">Сервис</span><h1>Услуги автосервиса RSService26</h1><p>Диагностика, ТО, двигатель, ходовая, тормоза, электрика, климат и установка запчастей для Hyundai/Kia.</p><div className="miniBenefits"><span>Сначала диагностика</span><span>Согласование цены до работ</span><span>Можно подобрать запчасти</span></div></section>{groups.map(g=><section className="section" key={g}><div className="sectionHead"><h2>{g}</h2><span className="badge">{services.filter(s=>s.group===g).length} услуг</span></div><div className="grid">{services.filter(s=>s.group===g).map(s=><article className="card" key={s.name}><h3>{s.name}</h3><p className="muted">{s.description}</p><p>Срок: <b>{s.time}</b></p><p className="price">{s.price}</p><div className="cardActions"><a className="btn primary" href={`/contact?type=service`}>Записаться</a><a className="btn" href="/availability">Подобрать запчасть</a></div></article>)}</div></section>)}</main><Footer/></>}